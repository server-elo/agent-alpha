# Secrets — .env holds the live Telegram bot token. Never commit.
.env
.env.local

# Build output
alpha
*.o

# Chat history / poll state
sessions/
tests/bin/

# Evolution run state: generation log and archived binaries. Also ignored so
# `git clean -fd` after a reverted generation cannot delete them.
evolution/
sandbox/

# Runtime logs
telegram.log
alpha-mcp
