# Secrets — .env holds the live Telegram bot token. Never commit.
.env
.env.local

# Build output
alpha
*.o

# Chat history / poll state
sessions/
tests/bin/

# Evolution run state: generation log is tracked; binary archives and sandboxes ignored
evolution/*
!evolution/log.jsonl
sandbox/

# Runtime logs
telegram.log
alpha-mcp
